# Point the dashboard's live transcript at the local fly-logs bridge.
# Start the bridge with: python log_bridge.py  (in deepgram-voice-agent-inbound-telephony)
# To go back to the deployed agent directly, remove this line (or set the wss URL).
VITE_AGENT_WS_URL=ws://localhost:8765/transcript
