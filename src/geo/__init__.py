# The GeoReferencer: fuses a pixel-space DetectorOutput with a CameraPose to produce
# the ground-referenced Observation the brain consumes. Geography lives here, in one
# place, so the detector stays swappable (interfaces.md locked decision #1).
